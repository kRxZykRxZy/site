# Codetorch (remake)

Run locally:
1. docker-compose up --build
2. Open http://localhost:8000

This is a minimal remake: FastAPI + Jinja2, SQLite (SQLAlchemy) with WAL, DB-backed write queue and CodeMirror v6 editor.
